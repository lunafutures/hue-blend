apiVersion: v1
kind: Service
metadata:
  name: {{ include "hue-chart.fullname" . }}
  labels:
    {{- include "hue-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hue-chart.selectorLabels" . | nindent 4 }}
